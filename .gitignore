# Python bytecode
__pycache__/
*.pyc

# Local notifier state
leaderboard_state.json
.leaderboard_state.json
.local_state/
.env

# Cached GitHub Actions state (structured_snapshot.json is committed for
# persistence; other state files remain ignored)
.github/state/leaderboard_state.json
.github/state/changelog_state.json
